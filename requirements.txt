Flask==3.1.0
PyMySQL==1.1.1
cryptography==48.0.1
python-dotenv==1.2.2
gunicorn==23.0.0

# TEMPORARY — deliberately vulnerable version, added only to verify the
# Trivy CRITICAL gate actually blocks a build. Not used anywhere in the
# app. REMOVE after confirming the gate fails, then push the revert.
PyYAML==5.3.1
