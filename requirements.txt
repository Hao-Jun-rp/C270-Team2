Flask==3.1.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Werkzeug==3.1.3
pytest==8.3.4

# Shared MySQL (Aiven) — only needed once you switch off local SQLite:
PyMySQL==1.1.1          # the MySQL driver SQLAlchemy talks through
cryptography==44.0.0    # required for MySQL 8's caching_sha2_password auth
python-dotenv==1.0.1    # loads DATABASE_URL etc. from your .env file
